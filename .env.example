# 数据库
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=inventory_reconcile

# JWT
JWT_SECRET=inventory-reconcile-secret
JWT_EXPIRES_IN=86400

# 前端地址（CORS）
FRONTEND_URL=http://localhost:8080

# 后端端口
PORT=3000
